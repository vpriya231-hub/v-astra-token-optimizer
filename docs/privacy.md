# Privacy

V-Astra is designed to support local-first optimization.

The core optimizer should not require uploading source code to a remote service. Any future remote features must be explicit and documented.

Secrets, credentials, access tokens, and environment files must be treated as sensitive context.
