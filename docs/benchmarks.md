# Benchmark Strategy

V-Astra benchmarks will report at least:

- original token estimate/count
- optimized token estimate/count
- absolute token savings
- savings percentage
- retention score
- execution time
- failure/fallback rate

A benchmark is considered useful only when token reduction and retained task-critical information are reported together.
